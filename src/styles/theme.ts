import { colors } from "./colors";

export const theme = {
  colors: {
    primary: colors.blue,
    secondary: colors.red,
    background: colors.white,
    fontPrimary: colors.darkBlue,
    fontSecondary: colors.lightBlue,
  },
};
